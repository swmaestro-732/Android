package com.chillsam.courmy.common.presentation.ui.token

import androidx.compose.ui.unit.dp

/**
 * 화면 좌우 여백. 기기 화면 끝과 콘텐츠 사이 간격이며 앱 전체가 같은 값을 쓴다.
 *
 * 상단 바·본문·하단 버튼 바가 모두 이 값을 써야 세로로 한 선이 맞는다. 카드·시트·드롭다운
 * **안쪽** 여백은 이 값이 아니다 — 그건 컴포넌트가 스스로 정한다.
 */
val ScreenHorizontalPadding = 22.dp
